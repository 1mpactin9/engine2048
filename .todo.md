1. Fix layout.css: duplicate .stage, powerups panel, is-armed, cancel, rotate-left, target-pulse ✅
2. Fix board.css: z-indexes, is-selecting + is-disabled combo ✅
3. Fix board.ts: setSize safety (exitSelectMode, layout retry, n clamp), layout --cell default ✅
4. Fix animate.ts: AU bounce tuning 0.3→0.15 ✅
5. Fix session.ts: undo double-debit, unlock reconcile, applyUnlocks >= ✅
6. Fix storage.ts: per-size save key + migration ✅
7. Fix app.ts: loadGame size mismatch, powerupRotate state machine, updateUI rotate setPower ✅
8. Add tests: unlock re-grant, undo double-debit, per-size save, powerupRotate state ✅
9. Run npm test, run npm run build ✅ (tests: 112, tsc: clean, build: TBD)
